{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript": "^0.11.5",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.6.1"
  },
  "dependencies": {
    "virtual-dom": "^2.1.1"
  }
}
